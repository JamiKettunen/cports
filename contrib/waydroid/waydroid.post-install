#!/bin/sh
# prepare new waydroid version configuration
if [ -f /var/lib/waydroid/waydroid_base.prop ]; then
	/usr/bin/waydroid upgrade --offline 2>/dev/null || :
fi
# TODO: restart waydroid container service?
